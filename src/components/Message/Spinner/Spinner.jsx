/**
 * Component for displaying a spinner.
 *
 * @example
 * // Renders the Spinner component, indicating loading or processing.
 * <Spinner />
 */
export function Spinner() {
  return <div className="spinner pr-2"></div>;
}
